# Phase 2 progress

- Standalone Core LangGraph is registry-backed.
- Real Notes agent execution is covered through the registry-backed graph factory.
- Production `graph/graph.py` remains unchanged for safety during migration.
