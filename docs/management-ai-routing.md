# Management AI routing

The management layer receives a normalized user request and selects one specialist boundary.

Boundaries: general conversation, voice I/O, English learning, news retrieval, and stock quotes.

The routing contract is provider-neutral so model selection can be added without coupling the gateway to a vendor.
