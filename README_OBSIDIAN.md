# Obsidian integration

Set `OBSIDIAN_VAULT_PATH` to the local or synced Obsidian vault directory to enable the vault bridge.

The bridge only reads and writes `.md` files below the configured vault root and rejects absolute paths and parent traversal.
