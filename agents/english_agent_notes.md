# English learning foundation

The agent is intentionally channel-neutral. Model behavior is injected through `build_english_responder`, so LINE, voice, Slack, and future channels can share the same learning contract.
